# Commits to skip in git blame: formatting sweeps, mass renames, anything that
# touches many lines without changing behaviour. Add the full 40-char hash.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub honours this file automatically.
